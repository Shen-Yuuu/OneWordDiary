# 单日分享恢复历史主题设计

## 规则

- 单日日记分享使用记录保存时的历史 `paperId`，包括从文字长卷进入详情后发起的分享。
- 最近七日分享继续使用当前设置主题，使一张长图保持统一色板。
- 单日日记存在背景图片时继续优先显示图片；图片不可用时回退到该日记的历史主题纯色。
- 不恢复旧的固定 SVG 底图，所有主题继续由 `styleForPaper()` 直接绘制。

## 验收

- 历史素灰日记在当前瑰粉主题下单日分享仍显示素灰。
- 同一状态下最近七日分享显示瑰粉。
- ArkTS 测试编译与 signed HAP 构建成功。

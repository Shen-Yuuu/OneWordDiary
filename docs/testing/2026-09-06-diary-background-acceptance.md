# 日记背景图验收记录

日期：2026-09-07。状态：功能实现、代码审查、自动测试和 Debug 打包完成；真机视觉与系统接口验收待执行。

## 交付行为
- 编辑区提供可选的背景图选择、更换、移除；每条日记独立持有图片。
- 首页、确认页、长卷进入的历史详情及单日分享卡片显示对应背景，未选或读取失败回退原有纯色。
- 草稿取消清理新图片，保存失败保留重试，修订成功后才移除旧图片；跨日返回图库和前台午夜刷新不继承昨日图片。
- 图片随备份导出和恢复；清空数据包含图片。保留旧版纯文字备份兼容性。
- 本期只接受静态图片。多帧图片明确提示暂不支持，分享仍输出静态 PNG。

## 环境与证据
- DevEco bundled Node 与 hvigor；工程 SDK 为 HarmonyOS 6.0.1 / API 21。
- 基线测试：48 项全部通过；本次最终全量测试：Tests run: 65, Failure: 0, Error: 0, Pass: 65, Ignore: 0。
- 测试日志：`.hvigor/background-integration-test.log`；结果：`OneWord_dev/entry/.test/default/intermediates/test/coverage_data/test_result.txt`。已核对用例结果，而非只依据 hvigor 退出码。
- 最终 Debug assembleHap 成功，包含 CompileArkTS、PackageHap、PackingCheck 和 SignHap；日志：`.hvigor/background-integration-build.log`。构建仍有 API 异常处理等警告，不宣称零警告。
- 产物：`OneWord_dev/entry/build/default/outputs/default/entry-default-signed.hap`（5309716 字节）。未覆盖仓库根目录旧安装包。
- `git diff --check` 通过；修改文本无 U+FFFD 替换字符；未改动权限、依赖、版本和签名配置。
- `hdc list targets -v` 只有 UART Ready 条目，没有 Connected 设备；未安装应用、清空真实数据或消耗用户修订机会。

## 验收矩阵
| 范围 | 已完成验证 | 尚需设备验证 |
| --- | --- | --- |
| 老数据升级 | nullable 列迁移代码审查；独立 SQLite 执行实际建表/ALTER SQL，12 个旧字段保留，新列为 NULL，新建/升级列集合一致 | HarmonyOS RDB 实际升级 |
| 日期隔离 | 双日期、跨日异步回调自动测试；前台午夜刷新代码审查 | 系统图库跨日与前后台生命周期 |
| 图片所有权 | 取消、更换、移除、保存失败重试、保存期间操作、提交后反馈异常自动测试 | 文件实际持久化及应用重启 |
| 原有规则 | 完整既有文字、备注、留白、修订、截止时间等测试通过 | 原有样式和触控回归 |
| 图片输入 | 编译与审查：20 MiB 源文件、80 × 1024 × 1024 像素、2048 长边、5 MiB 输出、单帧、方向修正、资源释放、合法 ID | ImageKit 实际解码、透明度、EXIF 方向、坏图与多帧拒绝 |
| 历史与分享 | 详情/单日数据传播测试；七日既有测试通过；解码完成事件与加载代次审查 | componentSnapshot 导出和接收端实际 PNG |
| 背景回退 | 无图分支保留；错误与超时纯色回退、重试、过期事件隔离代码审查 | 深浅色、不同设备上的视觉对照 |
| 备份 | v1/v2 结构往返、缺少图片字段、长度元数据异常、计划后冲突、事务失败文件回滚自动测试 | 实际图片往返、损坏图片导出拒绝、64 MiB 边界 |
| 清空 | 图片删除失败保留 reset_pending、启动重试自动测试；导入与清空代次保护审查 | 实际文件删除失败与在途原生解码 |
| 构建 | 65/65 用例通过，最终 Debug signed HAP 成功 | 真机启动与运行 |

新增测试中的图片存储使用 fake；这些测试验证业务编排、数据结构与回滚，不代表已验证真实图片字节或系统解码器。迁移的 SQLite 检查不替代 HarmonyOS RDB 验证。

## 待执行的真机检查
1. 选择横图、竖图、透明 PNG、含方向标记 JPEG；取消图库、替换、移除；删除原图库文件后应用副本仍可显示。
2. 保存、重启、修订、取消修订、历史详情；A/B 两日不同图片，C 日无图；跨午夜停留首页及图库返回。
3. 单日预览与接收的 PNG 对照；快速切换单日/七日后分享不混图、不透明、不漏背景；图片加载失败后重试。
4. 亮色/暗色，纸墨/素灰，大字号、小屏、键盘弹出；无图页面与旧版截图对照。
5. 图片备份导入独立测试实例并重启；缺失/损坏图片、磁盘写入失败、真实 RDB 迁移与事务回滚。
6. 仅在独立测试数据上验证清空，以及清空失败后的启动续做。
